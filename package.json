{
  "dependencies": {
    "axios": "^1.6.1",
    "body-parser": "^1.20.2",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "react-router-dom": "^6.18.0"
  },
  "scripts": {
    "start": "npm run server-run && npm run client-run  ",
    "client-run": "cd my-project && npm run dev",
    "server-run": "node server/server.js",
    "build": "npm run server && npm run client ",
    "server": "cd server && npm install",
    "client": "cd my-project && npm install &&npm run build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0"
  }
}
